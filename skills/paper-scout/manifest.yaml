name: paper-scout
kind: skill
category: retrieval
description: builds a profile-aware reading queue from scholarly and official sources
  before deep reading and synthesis.
links:
  calls:
  - research-profile
  reads_context:
  - project/AGENTS.md
capabilities:
  requires:
  - web.search
  - scholar.search
  - web.extract
artifacts:
  consumes:
  - research.request
  - research.profile
  produces:
  - corpus.queue
  - reading.queue
ui:
  group: retrieval
