# provider notes

Use capability bindings instead of hard-coded providers.

- `web.search` may bind to native search or Tavily
- `web.extract` and `web.crawl` may bind to Scrapling or browser tooling
- `scholar.search` may bind to host-native academic retrieval
- `citation.lookup` may bind to Crossref, Semantic Scholar, or host-native citation tooling
