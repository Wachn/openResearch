# Claude Code embedding sketch

- install or symlink `skills/` into the host skill directory
- expose `agents/profiles/` as role references or convert them into the host's preferred agent format
- inject `AGENTS.md` and `contexts/project/AGENTS.md`
- keep provider bindings in host config, not inside `SKILL.md`
- serve `apps/graph-studio/` locally for graph inspection
